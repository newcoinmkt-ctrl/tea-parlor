# AI 开发规则

本项目目标是 Telegram Bot + H5 Mini App 棋牌室，不是单个 Bot 小游戏。所有 AI 后续开发必须先读取：

1. `docs/00-项目索引/资料整理清单.md`
2. `docs/03-架构实施/棋牌室游戏开发说明.md`
3. `docs/04-AI开发规则/棋牌室AI开发规则.md`
4. 与本次任务相关的游戏规则文档

如果任务涉及从 GitHub 查找、拉取、评估或改造现成游戏代码，还必须读取：

5. `docs/04-AI开发规则/GitHub现成游戏获取设置.md`
6. `skills/github-game-importer/SKILL.md`

## 项目边界

- Bot 负责 Telegram 入口、通知、分享、客服和轻交互。
- H5 Mini App 负责大厅、牌桌、资产展示、任务、活动和实时对局。
- 游戏服务必须插件化，首发保留 `doudizhu`、`texas-holdem`、`mahjong` 三个游戏入口。
- 斗地主规则以 `docs/02-规则资料/JJ斗地主规则与玩法说明.md` 为首要依据。
- UI 方向参照 QQ 斗地主棋牌室：绿桌、木质/金色质感、三人座位、清晰场次入口、欢乐豆/积分表达。

## 资产与合规

- 默认只做影子积分、试玩币、内部账本和可审计流水。
- 不要默认接入真实充值、提现、博彩、赌场现金玩法或主网 USDT/TON。
- 任何真实数字货币能力必须先补齐合规、KYC/AML、地区限制、风控、资金托管、对账和提现审核方案。
- 不要把“可接入数字货币”写成“已经支持真金交易”。

## 工程红线

- 游戏规则引擎保持纯函数/纯状态机，不能直接读写数据库或钱包余额。
- 资产变化只能通过 Wallet/Ledger 服务完成，必须有流水、幂等键和可回放审计。
- 牌桌状态必须可重连、可回放、可托管。
- 后续新增游戏必须走统一 Game Adapter 接口，不能复制一套用户、钱包、场次和结算系统。
- 不读取或输出 `.env` 中的 Token、密钥、私钥。

## GitHub 现成游戏导入

- 从 GitHub 获取的游戏项目必须先登记到 `docs/05-GitHub候选游戏/候选仓库评估表.md`。
- 未完成 License、安全、IP、真钱/钱包逻辑检查前，不允许合入正式源码。
- 候选仓库只能先放在 `external/github-candidates/` 隔离目录。
- 可复用优先级：纯规则引擎、测试向量、adapter 思路、非品牌化 UI 布局。
- 禁止直接复制 QQ/JJ 品牌素材、真钱赌场源码、充值提现逻辑、远程执行脚本。

## 验证要求

- 斗地主规则变更必须补牌型、压制、叫分、结算测试。
- H5 UI 变更必须用 Playwright 或浏览器截图检查桌面和移动端。
- 涉及资产、登录、提现、后台权限的改动必须做安全审查和威胁建模。
- 对外文案必须区分已实现能力、规划能力、合规待确认能力。
