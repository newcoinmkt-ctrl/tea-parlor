{
  "name": "@tea-parlor/pinus-game-server",
  "version": "0.1.0",
  "private": true,
  "description": "Tea Parlor 棋牌室 Pinus 游戏服（基于 node-pinus）",
  "main": "./dist/app.js",
  "scripts": {
    "build": "tsc && node scripts/copy-config.js",
    "start": "npm run build && node dist/app.js",
    "dev": "npm run build && node dist/app.js",
    "test": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@types/bluebird": "^3.5.19",
    "@types/node": "^18.19.0",
    "bluebird": "^3.7.2",
    "pinus": "^1.7.4",
    "pinus-base-plugin": "^1.7.4",
    "reflect-metadata": "^0.2.2",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
